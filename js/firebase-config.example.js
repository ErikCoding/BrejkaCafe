/**
 * Rename this file to `firebase-config.js` and fill in the values
 * from your Firebase project settings (Project settings ⚙️ → General
 * → "Your apps" → SDK setup and configuration).
 *
 * Never commit real API keys to a public repo if you can avoid it —
 * for a simple café site this is low-risk (Firebase web config keys
 * are not secret), but it's still good practice to keep this file
 * out of version control if you'd rather not expose it.
 */
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
